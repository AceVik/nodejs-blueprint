{
  "name": "app",
  "version": "1.0.0",
  "description": "Production backend application.",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc && tsc-alias",
    "start": "node dist/index.js",
    "lint": "eslint . --ext .ts,.js",
    "lint:fix": "eslint . --ext .ts,.js --fix",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@micro/routes": "workspace:*",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "typescript": "^5.8.2",
    "tsc-alias": "^1.8.11",
    "tsx": "^4.19.3",
    "@types/node": "^22.13.9",
    "vitest": "^3.0.8"
  }
}
